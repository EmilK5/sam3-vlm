[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    integration: end-to-end tests across multiple pipeline components
    reproducibility: deterministic trace and configuration tests
    legacy: compatibility tests for pre-ASHT interfaces
