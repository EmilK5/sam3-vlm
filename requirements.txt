# Core (versions pinned by your SAM3 setup — install torch/transformers per the
# SAM3 repo instructions first, then the rest)
torch
transformers
numpy
opencv-python
pillow
matplotlib
scipy
gradio
# Verifier / agent
openai        # client only, for the OpenAI-compatible Qwen endpoint
# Benchmark datasets (orchestration_app.py: CountBench / PixMo-Count loaders)
datasets      # HuggingFace hub loaders for nielsr/countbench and allenai/pixmo-count
# Dev
pytest
requests
torchvision

